# VPS向け環境設定ファイル（軽量設定）
# 実際の値に変更してから .env としてリネームしてください

# === VPS固有設定 ===
VPS_MAX_CONCURRENT_TASKS=2        # 同時実行タスク数（VPS向け軽量化）
VPS_POSTS_PER_RUN=3              # 1回の実行での投稿数上限
VPS_MEMORY_LIMIT_MB=512          # メモリ使用量制限（MB）
VPS_DISK_CACHE_LIMIT_MB=100      # ディスクキャッシュ制限（MB）

# === API設定 ===
# FANZA API
FANZA_API_ID=your_fanza_api_id
FANZA_AFFILIATE_ID=your_affiliate_id

# Google Gemini API（キャラクター・顔認識用）
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.5-pro
GEMINI_RPM_LIMIT=15

# xAI Grok API（商品説明文生成用）
GROK_API_KEY=your_grok_api_key
GROK_BASE_URL=https://api.x.ai/v1
GROK_MODEL=grok-2-1212
GROK_RPM_LIMIT=50

# === WordPress設定 ===
WP_URL=https://your-wordpress-site.com
WP_USERNAME=your_wp_username
WP_PASSWORD=your_wp_app_password
WP_DOMAIN=your-wordpress-site.com

# === Google Sheets設定 ===
GOOGLE_SHEETS_ID=your_google_sheets_id
GOOGLE_SERVICE_ACCOUNT_FILE=config/wordpress-auto-post-service-account.json

# === Discord通知設定（オプション） ===
# DISCORD_WEBHOOK_URL=your_discord_webhook_url  # 不要のためコメントアウト

# === リトライ設定（VPS向け軽量化） ===
RETRY_ATTEMPTS=2                 # リトライ回数削減
RETRY_DELAY=60                   # リトライ間隔（秒）

# === ログ設定 ===
LOG_LEVEL=INFO                   # DEBUG, INFO, WARNING, ERROR
LOG_MAX_SIZE_MB=10              # ログファイル最大サイズ
LOG_BACKUP_COUNT=3              # ログファイル保持数

# === キャッシュ設定（軽量化） ===
CACHE_DIR=cache
CACHE_MAX_SIZE_MB=50            # キャッシュサイズ制限
CACHE_DEFAULT_EXPIRY=1800       # 30分（軽量化）
CACHE_IMAGE_EXPIRY=3600         # 1時間

# === セキュリティ設定 ===
SECURITY_MAX_RETRY_COUNT=3
SECURITY_LOCKOUT_DURATION=300

# === VPS向けパフォーマンス設定 ===
# 画像処理
IMAGE_RESIZE_QUALITY=80         # 画像品質下げて軽量化
IMAGE_MAX_SIZE_KB=500          # 画像サイズ制限

# HTTP設定
HTTP_TIMEOUT=30                 # タイムアウト短縮
HTTP_MAX_CONNECTIONS=5         # 接続数制限

# 顔認識設定（軽量化）
FACE_DETECTION_MAX_FACES=3     # 検出顔数制限
FACE_DETECTION_TIMEOUT=10      # 処理タイムアウト短縮